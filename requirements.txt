# Direct dependencies only. Transitive deps (numpy, requests, etc.) are resolved automatically by pip. 
# We don't pin them here to keep this file readable. Versions are pinned to what we tested against.
#
# Phase 1 - data ingestion & local storage
nba_api==1.11.4
pandas==3.0.3
duckdb==1.5.4

# Config (params.yml)
PyYAML==6.0.2

# Phase 2 - feature engineering
pyarrow==23.0.1

# Validation - parsing Basketball-Reference HTML tables for the ratings comparison
lxml==6.1.0

# Phase 2.5 - dbt transformation layer (replaces build_shots, build_player_team_seasons, features)
dbt-duckdb==1.9.3

# Phase 3 - modeling
scikit-learn==1.6.1
xgboost==2.1.4
joblib==1.4.2

# Testing & linting (also run in CI)
pytest==9.0.3
ruff==0.8.6

# Later phases will add:
#   streamlit      (Phase 4 - app)
